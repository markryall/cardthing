name = "graceful worker shutdown"
status = "done"
description = """
In 'cardthing work' (src/commands/work.rs): the first Ctrl-C finishes the current card then exits; a second Ctrl-C kills the agent child process, clears the in-flight card's owner (releasing the claim), and exits. When idle, exit immediately on the first Ctrl-C.

## Agent: cosmic-flamingo-32 (2026-07-14 03:03 UTC)

Card moved to `done`. Work complete: graceful shutdown now behaves as specified — idle Ctrl-C exits immediately, first Ctrl-C during a card finishes it then exits, second Ctrl-C kills the agent child, releases the card's claim, and exits."""
created_at = "2026-07-13T23:23:11.664812Z"
updated_at = "2026-07-14T03:03:57.333385Z"
tags = ["dogfood"]
